import styles from "@/app/styles/barberpole-bg.module.scss";

export default function Home() {
  return (
    <div className={`${styles.barberpoleBg} grow min-h-full`}>
      Home page
    </div>
  );
}
